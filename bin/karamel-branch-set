#!/bin/bash

if (($# == 0)); then
  echo "Usage: karamel-branch-set [master|dev]"
  exit 1
else
  branch="$1"
fi

case "$branch" in
  "master") git -C $KARAMEL_PATH switch master ;;
  "dev") git -C $KARAMEL_PATH switch dev ;;
  *) echo "Unknown branch: $branch"; exit 1; ;;
esac
