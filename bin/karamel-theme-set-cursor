#!/bin/bash

# Call the VSCode theme setter with Cursor-specific parameters
karamel-theme-set-vscode cursor "$HOME/.config/Cursor/User/settings.json" "$HOME/.local/state/karamel/toggles/skip-cursor-theme-changes" Cursor
