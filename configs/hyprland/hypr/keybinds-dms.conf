# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║                         DMS SHELL KEYBINDINGS                              ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

$mod = SUPER

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         🎨 DMS SHELL CONTROLS                             │
# └───────────────────────────────────────────────────────────────────────────┘

bindd = $mod, space, Lanceur DMS (Spotlight), exec, dms ipc call spotlight toggle
bindd = $mod, V, Presse-papier DMS, exec, dms ipc call clipboard toggle
bindd = $mod, M, Gestionnaire de processus, exec, dms ipc call processlist toggle
bindd = $mod, comma, Paramètres DMS, exec, dms ipc call settings toggle
bindd = $mod, N, Centre de notifications, exec, dms ipc call notifications toggle
bindd = $mod SHIFT, N, Bloc-notes DMS, exec, dms ipc call notepad toggle
bindd = $mod, Y, Sélecteur de fond d'écran, exec, dms ipc call dankdash wallpaper
bindd = $mod, TAB, Vue d'ensemble DMS, exec, dms ipc call hypr toggleOverview

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         📖 AIDE DMS                                       │
# └───────────────────────────────────────────────────────────────────────────┘

bindd = $mod SHIFT, Slash, Aide-mémoire DMS, exec, dms ipc call keybinds toggle hyprland

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         🔒 VERROUILLAGE DMS                               │
# └───────────────────────────────────────────────────────────────────────────┘

bindd = $mod, L, Verrouiller (DMS lock), exec, dms ipc call lock lock
bindd = $mod SHIFT, L, Verrouiller et suspendre, exec, dms ipc call lock lock && systemctl suspend
bindd = CTRL ALT, Delete, Gestionnaire de processus, exec, dms ipc call processlist toggle
