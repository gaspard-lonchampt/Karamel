# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║                         KARAMEL KEYBINDINGS                                ║
# ║              Universal keybinds for Hyprland & niri                        ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

$mod = SUPER

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         🚀 APPLICATIONS                                   │
# └───────────────────────────────────────────────────────────────────────────┘

bindd = $mod, T, Terminal (kitty), exec, kitty
bindd = $mod, Return, Terminal (kitty), exec, kitty
bindd = $mod CTRL, Return, Terminal flottant, exec, kitty --class kitty-floating
bindd = $mod, F, Gestionnaire de fichiers (Nemo), exec, nemo
bindd = $mod ALT, F, Explorateur terminal (Yazi), exec, kitty yazi
bindd = $mod, B, Navigateur (Zen Browser), exec, zen-browser
bindd = $mod SHIFT, Z, Éditeur de code (Zed), exec, zeditor
bindd = $mod CTRL, C, OpenCode (IA dans terminal), exec, kitty opencode
bindd = $mod, D, Discord (Vesktop), exec, vesktop
bindd = $mod, G, Telegram, exec, Telegram
bindd = $mod, S, Steam, exec, steam
bindd = $mod SHIFT, O, OBS Studio, exec, flatpak run com.obsproject.Studio
bindd = $mod CTRL, M, Moonlight (streaming), exec, moonlight

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         🎤 VOICE-TO-TEXT (WHISPERNOW)                     │
# └───────────────────────────────────────────────────────────────────────────┘

bindd = $mod SHIFT, D, Dictée vocale (WhisperNow), exec, kitty --class whispernow-floating ~/Karamel/scripts/whispernow.sh

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         🔊 AUDIO & MÉDIA                                  │
# └───────────────────────────────────────────────────────────────────────────┘

# Volume (Fn+F1/F2/F3)
bindeld = , XF86AudioRaiseVolume, Volume +, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 3%+
bindeld = , XF86AudioLowerVolume, Volume -, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 3%-
bindld = , XF86AudioMute, Mute/Unmute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindld = , XF86AudioMicMute, Mute micro, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle

# Media controls (Fn+F4/F5/F6)
bindld = , XF86AudioPrev, Piste précédente, exec, playerctl previous
bindld = , XF86AudioPlay, Lecture/Pause, exec, playerctl play-pause
bindld = , XF86AudioNext, Piste suivante, exec, playerctl next
bindld = , XF86AudioStop, Stop, exec, playerctl stop

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         🔆 LUMINOSITÉ                                     │
# └───────────────────────────────────────────────────────────────────────────┘

# Brightness (Fn+F7/F8)
bindeld = , XF86MonBrightnessUp, Luminosité +, exec, brightnessctl set 5%+
bindeld = , XF86MonBrightnessDown, Luminosité -, exec, brightnessctl set 5%-

# Workspace Overview (Fn+F9)
bindld = , XF86Display, Vue d'ensemble workspaces, exec, quickshell ipc -c qs-hyprview call expose toggle masonry

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         📸 CAPTURES D'ÉCRAN                               │
# └───────────────────────────────────────────────────────────────────────────┘

bindd = , Print, Capture zone (avec annotation), exec, ~/Karamel/scripts/screenshot.sh area
bindd = , XF86Launch1, Capture zone (avec annotation), exec, ~/Karamel/scripts/screenshot.sh area
bindd = CTRL, Print, Capture écran complet, exec, ~/Karamel/scripts/screenshot.sh screen
bindd = CTRL, XF86Launch1, Capture écran complet, exec, ~/Karamel/scripts/screenshot.sh screen
bindd = $mod SHIFT, S, Capture vers presse-papier, exec, ~/Karamel/scripts/screenshot.sh copy

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         🪟 GESTION FENÊTRES                               │
# └───────────────────────────────────────────────────────────────────────────┘

bindd = $mod, Q, Fermer la fenêtre, killactive
bindd = $mod SHIFT, F, Plein écran, fullscreen, 0
bindd = $mod, W, Basculer flottant, togglefloating
bindd = $mod SHIFT, W, Grouper fenêtres, togglegroup

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         🧭 NAVIGATION FENÊTRES                            │
# └───────────────────────────────────────────────────────────────────────────┘

bindd = $mod, left, Focus gauche, cyclenext
bindd = $mod, down, Focus bas, cyclenext
bindd = $mod, up, Focus haut, cyclenext, prev
bindd = $mod, right, Focus droite, cyclenext, prev
bindd = $mod, H, Focus gauche (vim), movefocus, l
bindd = $mod, J, Focus bas (vim), movefocus, d
bindd = $mod, K, Focus haut (vim), movefocus, u
bindd = $mod, Home, Focus première fenêtre, focuswindow, first
bindd = $mod, End, Focus dernière fenêtre, focuswindow, last

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         ↔️ DÉPLACEMENT FENÊTRES                           │
# └───────────────────────────────────────────────────────────────────────────┘

bindd = $mod SHIFT, left, Déplacer gauche, movewindow, l
bindd = $mod SHIFT, down, Déplacer bas, movewindow, d
bindd = $mod SHIFT, up, Déplacer haut, movewindow, u
bindd = $mod SHIFT, right, Déplacer droite, movewindow, r

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         🖥️ NAVIGATION MONITEURS                          │
# └───────────────────────────────────────────────────────────────────────────┘

bindd = $mod CTRL, left, Focus moniteur gauche, focusmonitor, l
bindd = $mod CTRL, right, Focus moniteur droite, focusmonitor, r
bindd = $mod CTRL, H, Focus moniteur gauche (vim), focusmonitor, l
bindd = $mod CTRL, J, Focus moniteur bas (vim), focusmonitor, d
bindd = $mod CTRL, K, Focus moniteur haut (vim), focusmonitor, u
bindd = $mod CTRL, L, Focus moniteur droite (vim), focusmonitor, r
bindd = $mod SHIFT CTRL, left, Envoyer vers moniteur gauche, movewindow, mon:l
bindd = $mod SHIFT CTRL, right, Envoyer vers moniteur droite, movewindow, mon:r

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         📑 WORKSPACES                                     │
# └───────────────────────────────────────────────────────────────────────────┘

# Navigation relative
bindd = $mod CTRL, down, Workspace suivant, workspace, r+1
bindd = $mod CTRL, up, Workspace précédent, workspace, r-1
bindd = $mod, U, Workspace suivant, workspace, r+1
bindd = $mod, I, Workspace précédent, workspace, r-1

# Déplacer vers workspace relatif
bindd = $mod ALT CTRL, down, Fenêtre vers workspace suivant, movetoworkspace, r+1
bindd = $mod ALT CTRL, up, Fenêtre vers workspace précédent, movetoworkspace, r-1
bindd = $mod CTRL, U, Fenêtre vers workspace suivant, movetoworkspace, e+1
bindd = $mod CTRL, I, Fenêtre vers workspace précédent, movetoworkspace, e-1
bindd = $mod SHIFT, Page_Down, Fenêtre vers workspace suivant, movetoworkspace, e+1
bindd = $mod SHIFT, Page_Up, Fenêtre vers workspace précédent, movetoworkspace, e-1
bindd = $mod SHIFT, U, Fenêtre vers workspace suivant, movetoworkspace, e+1
bindd = $mod SHIFT, I, Fenêtre vers workspace précédent, movetoworkspace, e-1

# Navigation molette
bind = $mod, mouse_up, workspace, r+1
bind = $mod, mouse_down, workspace, r-1
bind = $mod CTRL, mouse_up, movetoworkspace, r+1
bind = $mod CTRL, mouse_down, movetoworkspace, r-1

# Workspaces numérotés (AZERTY - rangée supérieure)
bindd = $mod, ampersand, Workspace 1, workspace, 1
bindd = $mod, eacute, Workspace 2, workspace, 2
bindd = $mod, quotedbl, Workspace 3, workspace, 3
bindd = $mod, apostrophe, Workspace 4, workspace, 4

# Workspaces numérotés (Pavé numérique)
bindd = $mod, KP_1, Workspace 1, workspace, 1
bindd = $mod, KP_2, Workspace 2, workspace, 2
bindd = $mod, KP_3, Workspace 3, workspace, 3
bindd = $mod, KP_4, Workspace 4, workspace, 4

# Déplacer vers workspace (AZERTY)
bindd = $mod SHIFT, ampersand, Envoyer vers workspace 1, movetoworkspace, 1
bindd = $mod SHIFT, eacute, Envoyer vers workspace 2, movetoworkspace, 2
bindd = $mod SHIFT, quotedbl, Envoyer vers workspace 3, movetoworkspace, 3
bindd = $mod SHIFT, apostrophe, Envoyer vers workspace 4, movetoworkspace, 4

# Déplacer vers workspace (Pavé numérique)
bindd = $mod SHIFT, KP_1, Envoyer vers workspace 1, movetoworkspace, 1
bindd = $mod SHIFT, KP_2, Envoyer vers workspace 2, movetoworkspace, 2
bindd = $mod SHIFT, KP_3, Envoyer vers workspace 3, movetoworkspace, 3
bindd = $mod SHIFT, KP_4, Envoyer vers workspace 4, movetoworkspace, 4

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         📐 REDIMENSIONNEMENT                              │
# └───────────────────────────────────────────────────────────────────────────┘

bindd = $mod, R, Cycle taille colonnes, layoutmsg, togglesplit
bindd = $mod, R, Agrandir colonne, layoutmsg, colresize +conf
bindd = $mod SHIFT, R, Réduire colonne, layoutmsg, colresize -conf
bindd = $mod CTRL, R, Réinitialiser taille, layoutmsg, colresize 1.0
binded = $mod, minus, Réduire largeur 10%, resizeactive, -10% 0
binded = $mod, equal, Agrandir largeur 10%, resizeactive, 10% 0
binded = $mod SHIFT, minus, Réduire hauteur 10%, resizeactive, 0 -10%
binded = $mod SHIFT, equal, Agrandir hauteur 10%, resizeactive, 0 10%
bindd = $mod, parenleft, Présélection gauche, layoutmsg, preselect l
bindd = $mod, parenright, Présélection droite, layoutmsg, preselect r

# Souris
bindmd = $mod, mouse:272, Déplacer fenêtre (souris), movewindow
bindmd = $mod, mouse:273, Redimensionner (souris), resizewindow
bindd = $mod, code:20, Réduire fenêtre, resizeactive, -100 0
bindd = $mod, code:21, Agrandir fenêtre, resizeactive, 100 0

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         🔧 OUTILS SHELL                                   │
# └───────────────────────────────────────────────────────────────────────────┘

# Pyprland
bindd = $mod SHIFT, T, Terminal dropdown (Pyprland), exec, pypr toggle term
bindd = $mod SHIFT, Return, Terminal dropdown (Pyprland), exec, pypr toggle term
bindd = $mod, Z, Zoom (Pyprland), exec, pypr zoom

# Hyprview
$layout = masonry
bindd = $mod, X, Vue d'ensemble workspaces, exec, quickshell ipc -c qs-hyprview call expose toggle $layout

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         🔋 POWER MODES                                    │
# └───────────────────────────────────────────────────────────────────────────┘

bindd = $mod CTRL, B, Cycle power (chill/notso/furious), exec, ~/Karamel/scripts/karamel-power.sh cycle

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         🔒 SYSTÈME                                        │
# └───────────────────────────────────────────────────────────────────────────┘

bindd = CTRL ALT, L, Verrouiller l'écran, exec, loginctl lock-session
bindd = $mod SHIFT, E, Quitter Hyprland, exit
bindd = $mod SHIFT, P, Éteindre les écrans, dpms, off
bindd = $mod, Colon, Menu des raccourcis (Walker), exec, ~/.config/karamel/scripts/keybinds.sh
