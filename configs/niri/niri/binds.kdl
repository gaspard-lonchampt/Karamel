// ╔═══════════════════════════════════════════════════════════════════════════╗
// ║                         KARAMEL KEYBINDINGS                                ║
// ║              Universal keybinds for Hyprland & niri                        ║
// ╚═══════════════════════════════════════════════════════════════════════════╝

binds {
    // Mod-Shift-/, which is usually the same as Mod-?,
    // shows a list of important hotkeys.
    Mod+Shift+Slash { show-hotkey-overlay; }

    // ┌───────────────────────────────────────────────────────────────────────────┐
    // │                         🚀 APPLICATIONS                                   │
    // └───────────────────────────────────────────────────────────────────────────┘

    Mod+Space hotkey-overlay-title="Lanceur DMS" { spawn "dms" "ipc" "call" "spotlight" "toggle"; }
    Mod+Ctrl+Space hotkey-overlay-title="Vue d'ensemble DMS" { spawn "dms" "ipc" "call" "dash" "open" "overview"; }
    Mod+T hotkey-overlay-title="Terminal (kitty)" { spawn "kitty"; }
    Mod+Return hotkey-overlay-title="Terminal (kitty)" { spawn "kitty"; }
    Mod+Ctrl+Return hotkey-overlay-title="Terminal flottant" { spawn "kitty" "--class=floating-kitty"; }
    Mod+F hotkey-overlay-title="Gestionnaire de fichiers (Nemo)" { spawn "nemo"; }
    Mod+Alt+F hotkey-overlay-title="Explorateur terminal (Yazi)" { spawn "kitty" "yazi"; }
    Mod+B hotkey-overlay-title="Navigateur (Zen Browser)" { spawn "zen-browser"; }
    Mod+Shift+Z hotkey-overlay-title="Éditeur de code (Zed)" { spawn "sh" "-c" "$HOME/.config/niri/Scripts/zed-resize-fix.sh"; }
    Mod+Ctrl+C hotkey-overlay-title="OpenCode (IA dans terminal)" { spawn "kitty" "opencode"; }
    Mod+D hotkey-overlay-title="Discord (Vesktop)" { spawn "vesktop"; }
    Mod+G hotkey-overlay-title="Telegram" { spawn "Telegram"; }
    Mod+S hotkey-overlay-title="Steam" { spawn "steam"; }
    Mod+Shift+O hotkey-overlay-title="OBS Studio" { spawn "flatpak" "run" "com.obsproject.Studio"; }
    Mod+Ctrl+M hotkey-overlay-title="Moonlight (streaming)" { spawn "moonlight"; }

    // ┌───────────────────────────────────────────────────────────────────────────┐
    // │                         🔊 AUDIO & MÉDIA                                  │
    // └───────────────────────────────────────────────────────────────────────────┘

    XF86AudioRaiseVolume allow-when-locked=true hotkey-overlay-title="Volume +" { spawn-sh "wpctl set-volume @DEFAULT_AUDIO_SINK@ 0.03+ -l 1.0"; }
    XF86AudioLowerVolume allow-when-locked=true hotkey-overlay-title="Volume -" { spawn-sh "wpctl set-volume @DEFAULT_AUDIO_SINK@ 0.03-"; }
    XF86AudioMute        allow-when-locked=true hotkey-overlay-title="Mute/Unmute" { spawn-sh "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle"; }
    XF86AudioMicMute     allow-when-locked=true hotkey-overlay-title="Mute micro" { spawn-sh "wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle"; }
    XF86AudioPlay        allow-when-locked=true hotkey-overlay-title="Lecture/Pause" { spawn-sh "playerctl play-pause"; }
    XF86AudioStop        allow-when-locked=true hotkey-overlay-title="Stop lecture" { spawn-sh "playerctl stop"; }
    XF86AudioPrev        allow-when-locked=true hotkey-overlay-title="Piste précédente" { spawn-sh "playerctl previous"; }
    XF86AudioNext        allow-when-locked=true hotkey-overlay-title="Piste suivante" { spawn-sh "playerctl next"; }

    // ┌───────────────────────────────────────────────────────────────────────────┐
    // │                         🔆 LUMINOSITÉ                                     │
    // └───────────────────────────────────────────────────────────────────────────┘

    XF86MonBrightnessUp allow-when-locked=true hotkey-overlay-title="Luminosité +" { spawn "brightnessctl" "--class=backlight" "set" "+5%"; }
    XF86MonBrightnessDown allow-when-locked=true hotkey-overlay-title="Luminosité -" { spawn "brightnessctl" "--class=backlight" "set" "5%-"; }

    // Workspace Overview (Fn+F9)
    XF86Display hotkey-overlay-title="Vue d'ensemble workspaces" { toggle-overview; }

    // ┌───────────────────────────────────────────────────────────────────────────┐
    // │                         🪟 GESTION FENÊTRES                               │
    // └───────────────────────────────────────────────────────────────────────────┘

    Mod+X repeat=false hotkey-overlay-title="Vue d'ensemble workspaces" { toggle-overview; }
    Mod+Q repeat=false hotkey-overlay-title="Fermer la fenêtre" { close-window; }
    Mod+Shift+Q repeat=false hotkey-overlay-title="Fermer le workspace" { spawn "sh" "-c" "~/Karamel/scripts/karamel-workspace.sh kill"; }

    // ┌───────────────────────────────────────────────────────────────────────────┐
    // │                         🧭 NAVIGATION FENÊTRES                            │
    // └───────────────────────────────────────────────────────────────────────────┘

    Mod+Left  hotkey-overlay-title="Focus gauche" { focus-column-left; }
    Mod+Down  hotkey-overlay-title="Focus bas" { focus-window-down; }
    Mod+Up    hotkey-overlay-title="Focus haut" { focus-window-up; }
    Mod+Right hotkey-overlay-title="Focus droite" { focus-column-right; }
    Mod+H     hotkey-overlay-title="Focus gauche (vim)" { focus-column-left; }
    Mod+J     hotkey-overlay-title="Focus bas (vim)" { focus-window-down; }
    Mod+K     hotkey-overlay-title="Focus haut (vim)" { focus-window-up; }
    Mod+L     hotkey-overlay-title="Focus droite (vim)" { focus-column-right; }
    Mod+Home  hotkey-overlay-title="Focus première colonne" { focus-column-first; }
    Mod+End   hotkey-overlay-title="Focus dernière colonne" { focus-column-last; }

    // ┌───────────────────────────────────────────────────────────────────────────┐
    // │                         ↔️ DÉPLACEMENT FENÊTRES                           │
    // └───────────────────────────────────────────────────────────────────────────┘

    Mod+Shift+Left  hotkey-overlay-title="Déplacer colonne gauche" { move-column-left; }
    Mod+Shift+Right hotkey-overlay-title="Déplacer colonne droite" { move-column-right; }
    Mod+Ctrl+H      hotkey-overlay-title="Déplacer colonne gauche (vim)" { move-column-left; }
    Mod+Ctrl+J      hotkey-overlay-title="Déplacer fenêtre bas (vim)" { move-window-down; }
    Mod+Ctrl+K      hotkey-overlay-title="Déplacer fenêtre haut (vim)" { move-window-up; }
    Mod+Ctrl+L      hotkey-overlay-title="Déplacer colonne droite (vim)" { move-column-right; }
    Mod+Ctrl+Home   hotkey-overlay-title="Colonne en premier" { move-column-to-first; }
    Mod+Ctrl+End    hotkey-overlay-title="Colonne en dernier" { move-column-to-last; }

    // ┌───────────────────────────────────────────────────────────────────────────┐
    // │                         🖥️ NAVIGATION MONITEURS                          │
    // └───────────────────────────────────────────────────────────────────────────┘

    Mod+Ctrl+Left   hotkey-overlay-title="Focus moniteur gauche" { focus-monitor-left; }
    Mod+Shift+Down  hotkey-overlay-title="Focus moniteur bas" { focus-monitor-down; }
    Mod+Shift+Up    hotkey-overlay-title="Focus moniteur haut" { focus-monitor-up; }
    Mod+Ctrl+Right  hotkey-overlay-title="Focus moniteur droite" { focus-monitor-right; }
    Mod+Shift+H     hotkey-overlay-title="Focus moniteur gauche (vim)" { focus-monitor-left; }
    Mod+Shift+J     hotkey-overlay-title="Focus moniteur bas (vim)" { focus-monitor-down; }
    Mod+Shift+K     hotkey-overlay-title="Focus moniteur haut (vim)" { focus-monitor-up; }
    Mod+Shift+L     hotkey-overlay-title="Focus moniteur droite (vim)" { focus-monitor-right; }

    Mod+Shift+Ctrl+Left  hotkey-overlay-title="Envoyer vers moniteur gauche" { move-column-to-monitor-left; }
    Mod+Shift+Ctrl+Down  hotkey-overlay-title="Envoyer vers moniteur bas" { move-column-to-monitor-down; }
    Mod+Shift+Ctrl+Up    hotkey-overlay-title="Envoyer vers moniteur haut" { move-column-to-monitor-up; }
    Mod+Shift+Ctrl+Right hotkey-overlay-title="Envoyer vers moniteur droite" { move-column-to-monitor-right; }
    Mod+Shift+Ctrl+H     hotkey-overlay-title="Envoyer vers moniteur gauche (vim)" { move-column-to-monitor-left; }
    Mod+Shift+Ctrl+J     hotkey-overlay-title="Envoyer vers moniteur bas (vim)" { move-column-to-monitor-down; }
    Mod+Shift+Ctrl+K     hotkey-overlay-title="Envoyer vers moniteur haut (vim)" { move-column-to-monitor-up; }
    Mod+Shift+Ctrl+L     hotkey-overlay-title="Envoyer vers moniteur droite (vim)" { move-column-to-monitor-right; }

    // ┌───────────────────────────────────────────────────────────────────────────┐
    // │                         📑 WORKSPACES                                     │
    // └───────────────────────────────────────────────────────────────────────────┘

    // Navigation relative
    Mod+Ctrl+Down      hotkey-overlay-title="Workspace suivant" { focus-workspace-down; }
    Mod+Ctrl+Up        hotkey-overlay-title="Workspace précédent" { focus-workspace-up; }
    Mod+U              hotkey-overlay-title="Workspace suivant" { focus-workspace-down; }
    Mod+I              hotkey-overlay-title="Workspace précédent" { focus-workspace-up; }

    // Navigation directe AZERTY (1-4)
    Mod+ampersand      hotkey-overlay-title="Workspace 1" { focus-workspace 1; }
    Mod+eacute         hotkey-overlay-title="Workspace 2" { focus-workspace 2; }
    Mod+quotedbl       hotkey-overlay-title="Workspace 3" { focus-workspace 3; }
    Mod+apostrophe     hotkey-overlay-title="Workspace 4" { focus-workspace 4; }

    // Déplacer colonne vers workspace AZERTY
    Mod+Shift+ampersand    hotkey-overlay-title="Envoyer vers workspace 1" { move-column-to-workspace 1; }
    Mod+Shift+eacute       hotkey-overlay-title="Envoyer vers workspace 2" { move-column-to-workspace 2; }
    Mod+Shift+quotedbl     hotkey-overlay-title="Envoyer vers workspace 3" { move-column-to-workspace 3; }
    Mod+Shift+apostrophe   hotkey-overlay-title="Envoyer vers workspace 4" { move-column-to-workspace 4; }

    // Déplacer vers workspace relatif
    Mod+Ctrl+Page_Down hotkey-overlay-title="Colonne vers workspace suivant" { move-column-to-workspace-down; }
    Mod+Ctrl+Page_Up   hotkey-overlay-title="Colonne vers workspace précédent" { move-column-to-workspace-up; }
    Mod+Ctrl+U         hotkey-overlay-title="Colonne vers workspace suivant" { move-column-to-workspace-down; }
    Mod+Ctrl+I         hotkey-overlay-title="Colonne vers workspace précédent" { move-column-to-workspace-up; }
    Mod+Ctrl+Alt+Down  hotkey-overlay-title="Fenêtre vers workspace suivant" { move-window-to-workspace-down; }
    Mod+Ctrl+Alt+Up    hotkey-overlay-title="Fenêtre vers workspace précédent" { move-window-to-workspace-up; }
    Mod+Shift+Page_Down hotkey-overlay-title="Déplacer workspace bas" { move-workspace-down; }
    Mod+Shift+Page_Up   hotkey-overlay-title="Déplacer workspace haut" { move-workspace-up; }
    Mod+Shift+U         hotkey-overlay-title="Déplacer workspace bas" { move-workspace-down; }
    Mod+Shift+I         hotkey-overlay-title="Déplacer workspace haut" { move-workspace-up; }

    // Navigation molette
    Mod+WheelScrollDown      cooldown-ms=150 { focus-workspace-down; }
    Mod+WheelScrollUp        cooldown-ms=150 { focus-workspace-up; }
    Mod+Ctrl+WheelScrollDown cooldown-ms=150 { move-column-to-workspace-down; }
    Mod+Ctrl+WheelScrollUp   cooldown-ms=150 { move-column-to-workspace-up; }
    Mod+WheelScrollRight      { focus-column-right; }
    Mod+WheelScrollLeft       { focus-column-left; }
    Mod+Ctrl+WheelScrollRight { move-column-right; }
    Mod+Ctrl+WheelScrollLeft  { move-column-left; }
    Mod+Shift+WheelScrollDown      { focus-column-right; }
    Mod+Shift+WheelScrollUp        { focus-column-left; }
    Mod+Ctrl+Shift+WheelScrollDown { move-column-right; }
    Mod+Ctrl+Shift+WheelScrollUp   { move-column-left; }

    // Workspaces numérotés (Pavé numérique)
    // Note: KP_End/KP_Down/etc au lieu de KP_1/KP_2/etc car bug xkbcommon (même scancode)
    Mod+KP_End   hotkey-overlay-title="Workspace 1" { focus-workspace 1; }
    Mod+KP_Down  hotkey-overlay-title="Workspace 2" { focus-workspace 2; }
    Mod+KP_Next  hotkey-overlay-title="Workspace 3" { focus-workspace 3; }
    Mod+KP_Left  hotkey-overlay-title="Workspace 4" { focus-workspace 4; }

    // Déplacer vers workspace (Pavé numérique)
    Mod+Ctrl+KP_End   hotkey-overlay-title="Envoyer vers workspace 1" { move-column-to-workspace 1; }
    Mod+Ctrl+KP_Down  hotkey-overlay-title="Envoyer vers workspace 2" { move-column-to-workspace 2; }
    Mod+Ctrl+KP_Next  hotkey-overlay-title="Envoyer vers workspace 3" { move-column-to-workspace 3; }
    Mod+Ctrl+KP_Left  hotkey-overlay-title="Envoyer vers workspace 4" { move-column-to-workspace 4; }

    // ┌───────────────────────────────────────────────────────────────────────────┐
    // │                         📐 REDIMENSIONNEMENT                              │
    // └───────────────────────────────────────────────────────────────────────────┘

    Mod+Parenleft  hotkey-overlay-title="Absorber/expulser fenêtre gauche" { consume-or-expel-window-left; }
    Mod+Parenright hotkey-overlay-title="Absorber/expulser fenêtre droite" { consume-or-expel-window-right; }
    Mod+Comma        hotkey-overlay-title="Absorber fenêtre dans colonne" { consume-window-into-column; }
    Mod+Period       hotkey-overlay-title="Expulser fenêtre de colonne" { expel-window-from-column; }
    Mod+R            hotkey-overlay-title="Cycle taille colonnes" { switch-preset-column-width; }
    Mod+Shift+R      hotkey-overlay-title="Cycle hauteur fenêtres" { switch-preset-window-height; }
    Mod+Ctrl+R       hotkey-overlay-title="Réinitialiser hauteur" { reset-window-height; }
    Mod+Ctrl+F       hotkey-overlay-title="Maximiser colonne" { maximize-column; }
    Mod+Shift+F      hotkey-overlay-title="Plein écran" { fullscreen-window; }
    Mod+C            hotkey-overlay-title="Centrer colonne" { center-column; }
    Mod+Alt+C        hotkey-overlay-title="Centrer colonnes visibles" { center-visible-columns; }
    Mod+Minus        hotkey-overlay-title="Réduire largeur 10%" { set-column-width "-10%"; }
    Mod+Equal        hotkey-overlay-title="Agrandir largeur 10%" { set-column-width "+10%"; }
    Mod+Shift+Minus  hotkey-overlay-title="Réduire hauteur 10%" { set-window-height "-10%"; }
    Mod+Shift+Equal  hotkey-overlay-title="Agrandir hauteur 10%" { set-window-height "+10%"; }
    Mod+W            hotkey-overlay-title="Basculer flottant" { toggle-window-floating; }
    Mod+Shift+W      hotkey-overlay-title="Focus flottant/tiling" { switch-focus-between-floating-and-tiling; }
    Mod+V            hotkey-overlay-title="Mode onglets colonne" { toggle-column-tabbed-display; }

    // ┌───────────────────────────────────────────────────────────────────────────┐
    // │                         📸 CAPTURES D'ÉCRAN                               │
    // └───────────────────────────────────────────────────────────────────────────┘

    Print       hotkey-overlay-title="Capture zone (avec annotation)" { spawn "sh" "-c" "~/Karamel/scripts/screenshot.sh area"; }
    Mod+Shift+S hotkey-overlay-title="Capture vers presse-papier" { spawn "sh" "-c" "~/Karamel/scripts/screenshot.sh copy"; }
    Ctrl+Print  hotkey-overlay-title="Capture écran complet" { spawn "sh" "-c" "~/Karamel/scripts/screenshot.sh screen"; }

    // ┌───────────────────────────────────────────────────────────────────────────┐
    // │                         🔋 POWER MODES                                    │
    // └───────────────────────────────────────────────────────────────────────────┘

    Mod+Ctrl+B hotkey-overlay-title="Cycle power: chill → notso → furious" { spawn-sh "~/Karamel/scripts/karamel-power.sh cycle"; }

    // ┌───────────────────────────────────────────────────────────────────────────┐
    // │                         🔒 SYSTÈME                                        │
    // └───────────────────────────────────────────────────────────────────────────┘

    Ctrl+Alt+L       hotkey-overlay-title="Verrouiller l'écran" { spawn "loginctl" "lock-session"; }
    Mod+Escape allow-inhibiting=false hotkey-overlay-title="Basculer inhibiteur clavier" { toggle-keyboard-shortcuts-inhibit; }
    Mod+Shift+E      hotkey-overlay-title="Quitter niri" { quit; }
    Ctrl+Alt+Delete  hotkey-overlay-title="Quitter niri" { quit; }
    Mod+Shift+P      hotkey-overlay-title="Éteindre les écrans" { power-off-monitors; }
    Mod+Colon        hotkey-overlay-title="Menu des raccourcis (Walker)" { spawn-sh "$HOME/.config/karamel/scripts/keybinds.sh"; }
}
