# Tab 1: Runner (free terminal)
new_tab runner
cd /home/sarp/Project/Mobilic
launch fish

# Tab 2: Git (split vertical)
new_tab git
cd /home/sarp/Project/Mobilic/mobilic
launch fish
launch --location=vsplit --cwd=/home/sarp/Project/Mobilic/mobilic-api fish

# Tab 3: Chrome MCP
new_tab chrome mcp
cd /home/sarp/Project/Mobilic
launch fish -c "claude --chrome; exec fish"

# Tab 4: App (Frontend | Backend)
new_tab app
cd /home/sarp/Project/Mobilic/mobilic
launch --title "Frontend" fish -c "pnpm start; exec fish"
launch --location=vsplit --cwd=/home/sarp/Project/Mobilic/mobilic-api --title "Backend" fish -c "DOTENV_FILE=.env.local pipenv run flask run --host 0.0.0.0; exec fish"

# Tab 5: DB (Free | Docker)
new_tab db
cd /home/sarp/Project/Mobilic/mobilic-api
launch fish
launch --location=vsplit --cwd=/home/sarp/Project/Mobilic/mobilic-api --title "Docker" fish -c "docker compose up mobilic-db mobilic-db-test redis; exec fish"

# Tab 6: System monitor
new_tab btop
cd /home/sarp/Project/Mobilic
launch fish -c "btop; exec fish"
